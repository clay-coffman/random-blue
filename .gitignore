# Per-session agent transient state
.claude/scheduled_tasks.lock

# Engagement-specific notes (sales pitches, demo scripts, talking
# points, meeting prep). The repo is public; engineering docs live in
# docs/, anything in docs/_internal/ stays out of the public surface.
# Add new sales/communication docs to docs/_internal/, not docs/.
docs/_internal/
docs/talking-points.md

# MCP config — keep local or symlinked from a trusted local source
.mcp.json

# Codex local marker
.codex

# Per-worktree env (collision-avoidance ports + derived connection strings)
.env.local

# Playwright MCP transient state
.playwright-mcp/
# agent-browser / ui-testing screenshots
screenshots/

# Node
node_modules/
.npm/
*.log
.DS_Store

# Local env
.env
.env.*.local
!.env.example

# Wrangler local secrets (read by getPlatformProxy / initOpenNextCloudflareForDev)
.dev.vars
!.dev.vars.example

# Next.js
.next/
out/
*.tsbuildinfo
next-env.d.ts

# OpenNext / Wrangler / Cloudflare
.open-next/
.wrangler/
worker-configuration.d.ts
cloudflare-env.d.ts
